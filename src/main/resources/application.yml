server:
  port: 8083

spring:
  application:
    name: matching-service
  datasource:
    url: jdbc:h2:mem:testdb
    driverClassName: org.h2.Driver
    username: sa
    password: 
  h2:
    console:
      enabled: true

services:
  location-service:
    url: http://localhost:8081 # 위치 정보 서비스 주소
  driver-service:
    url: http://localhost:8082 # 기사 서비스 주소